const TILT_ANGLE = Math.atan(2 ** 0.5);
export const PIXEL_PER_GRID = 64;
export const COS = Math.cos(Math.PI * 2.25 / 9);
export const SIN = Math.sin(Math.PI * 2.25 / 9);
export const COS_X = Math.cos(TILT_ANGLE);
export const MIN_ZOOM = 1;
export const MAX_ZOOM = 4;
